##### hc-noticebar

消息通知控件！

###### 普通用法
根据内容判断是否滚动！

``` html
  <hc-noticebar>
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
  <hc-noticebar>
    <p>因为类似这样的赋值，会使本来和</p>
  </hc-noticebar>
```
<div class="phone">
  <hc-noticebar>
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
  <hc-noticebar>
    <p>因为类似这样的赋值，会使本来和</p>
  </hc-noticebar>
</div>

###### 普通用法
`scrollable="false"`手动禁止滚动！

``` html
  <hc-noticebar scrollable="false">
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
```
<div class="phone">
  <hc-noticebar scrollable="false">
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
</div>

###### 带图标
`icon`给前面添加图标～

``` html
  <hc-noticebar icon="notice">
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
```
<div class="phone">
  <hc-noticebar icon="notice">
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
</div>

###### 带图标
`closable`可关闭！

``` html
  <hc-noticebar icon="notice" closable>
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
```
<div class="phone">
  <hc-noticebar icon="notice" closable>
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
</div>

###### 自定义颜色
`color`自定义主题颜色！

``` html
  <hc-noticebar icon="notice" closable color="#f00">
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
```
<div class="phone">
  <hc-noticebar icon="notice" closable color="#f00">
    <p>因为类似这样的赋值，会使本来和 module.exports 指向同一内容的 exports 失效，如果想要 exports 再次和 module.exports 指向同一内容，则需要再次给 exports 赋值</p>
  </hc-noticebar>
</div>

<!-- Auto Generated Below -->


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
